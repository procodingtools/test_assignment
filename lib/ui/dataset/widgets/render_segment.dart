import 'package:flutter/material.dart';

class RenderSegment extends StatelessWidget {
  const RenderSegment({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
